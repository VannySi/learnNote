<!--<view class="container">
  <view  bindtap="bindViewTap" class="logo">
    <image class="logo-img" src="{{userInfo.avatarUrl}}" background-size="cover"></image>
  </view>
  <view class="login">
    <form bindsubmit="formSubmit">
      <view class="login-item">
        <label>手机号</label>
        <input type="number" bindinput="bindKeyInput" placeholder="请输入手机号" maxlength="11"/>
        <button class="verify" bindtap="getVerify">获取验证码</button>
      </view>
      <view class="login-item login-shadow">
        <label>验证码</label>
        <input type="number" placeholder="请输入验证码"/>
      </view>
      <view>
        <button class="margin-center" type="primary" size="default" formType="submit"> 登　录 </button>
      </view>
    </form>
    <view class="margin-center flex-row">
      <label for="pwd">
        <checkbox id='pwd' value="记住密码"/>记住密码
      </label>
      <view class="turn" bindtap="register">注册</view>
    </view>
  </view>
</view>-->
<view class="container">
  <view class="userinfo">
    <image class="userinfo-avatar" src="{{userInfo.avatarUrl}}" background-size="cover"></image>
    <text class="userinfo-nickname">{{userInfo.nickName}}</text>
  </view>
  <view class="usermotto">
    <text class="user-motto {{loginChange > 0 ? 'font-success' : (loginChange < 0 ? '' : 'font-danger')}}">{{motto}}</text>
  </view>
</view>